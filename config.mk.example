# Central deployment config (single source of truth)
# Copy this file to config.mk and edit with your values
#
# Usage:
#   cp config.mk.example config.mk
#   # Edit config.mk with your domain, region, and allowed origins
#   make deploy

# AWS Region for your deployment (e.g., us-east-1, eu-west-2)
REGION = eu-west-2

# Root domain (for email addresses: user@domain.com)
# JMAP server will be deployed at:
#   - jmap.yourdomain.com (JMAP API)
#   - yourdomain.com/.well-known/jmap (autodiscovery redirect)
ROOT_DOMAIN = example.com

# Comma-separated allowed CORS origins
# Add all web clients that will use this server
# Examples:
#   - https://yourdomain.com (web client at root)
#   - https://app.yourdomain.com (web client at subdomain)
#   - https://other-domain.com (web client at different domain)
#   - http://localhost:5173 (local development)
ALLOWED_ORIGINS = https://example.com,http://localhost:5173
